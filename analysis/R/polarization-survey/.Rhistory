treated_polarization = rnorm(400, mean = 2.5, sd = 1),
choice = sample(c("original", "treated"), 400, replace = TRUE)
)
head(data)
data <- data.frame(
participant_id = rep(1:100, each = 4),  # 100 participants, 4 text pairs each
text_pair_id = rep(1:4, times = 100),   # Text pairs: 1-4 per participant
treatment = rep(c("LLM", "Human", "Placebo"), each = 4, times = 100),
tweet_bias = rep(c("left", "right"), times = 200),  # Alternating left/right biases
political_leaning = sample(c("liberal", "moderate", "conservative"), 400, replace = TRUE),
original_polarization = rnorm(400, mean = 3, sd = 1),
treated_polarization = rnorm(400, mean = 2.5, sd = 1),
choice = sample(c("original", "treated"), 400, replace = TRUE)
)
sample(data)
# binary outcome: 1 = treated is polarized, 0 = otherwise
data$treated_less_polarized <- ifelse(data$choice == "treated", 1, 0)
model_rq1 <- glmer(treated_less_polarized ~ treatment + tweet_bias + political_leaning + (1 | participant_id) + (1 | text_pair_id), data = data, family = "binomial")
summary(model_rq1)
report(model_rq1)
# polarization difference (original - treated)
data$polarization_diff <- data$original_polarization - data$treated_polarization
# compare LLM vs. Placebo
data_llm_vs_placebo <- subset(data, treatment %in% c("LLM", "Placebo"))
model_rq2 <- lmer(polarization_diff ~ treatment + tweet_bias + political_leaning + (1 | participant_id) + (1 | text_pair_id), data = data_llm_vs_placebo)
summary(model_rq2)
report(model_rq2)
# compare LLM vs Human
data_llm_vs_human <- subset(data, treatment %in% c("LLM", "Human"))
model_rq3 <- lmer(polarization_diff ~ treatment + tweet_bias + political_leaning +
(1 | participant_id) + (1 | text_pair_id),
data = data_llm_vs_human)
summary(model_rq3)
report(model_rq3)
model_rq4 <- lmer(original_polarization ~ tweet_bias * political_leaning +
(1 | participant_id) + (1 | text_pair_id), data = data)
summary(model_rq4)
report(model_rq4)
data <- data.frame(
participant_id = rep(1:100, each = 4),  # 100 participants, 4 text pairs each
treatment = rep(c("LLM", "Human", "Placebo"), each = 4, times = 100),
tweet_bias = rep(c("left", "right"), times = 200),  # Alternating left/right biases
political_leaning = sample(c("liberal", "moderate", "conservative"), 400, replace = TRUE),
original_polarization = rnorm(400, mean = 3, sd = 1),
treated_polarization = rnorm(400, mean = 2.5, sd = 1),
choice = sample(c("original", "treated"), 400, replace = TRUE)
)
sample(data)
# binary outcome: 1 = treated is polarized, 0 = otherwise
model_rq1 <- glmer(treated_less_polarized ~ treatment + tweet_bias + political_leaning +
(1 | participant_id),
data = data, family = "binomial")
# binary outcome: 1 = treated is polarized, 0 = otherwise
data$treated_less_polarized <- ifelse(data$choice == "treated", 1, 0)
model_rq1 <- glmer(treated_less_polarized ~ treatment + tweet_bias + political_leaning +
(1 | participant_id),
data = data, family = "binomial")
summary(model_rq1)
report(model_rq1)
# Polarization difference (original - treated)
data$polarization_diff <- data$original_polarization - data$treated_polarization
# Compare LLM vs. Placebo
data_llm_vs_placebo <- subset(data, treatment %in% c("LLM", "Placebo"))
model_rq2 <- lmer(polarization_diff ~ treatment + tweet_bias + political_leaning +
(1 | participant_id),
data = data_llm_vs_placebo)
summary(model_rq2)
# Compare LLM vs. Human
data_llm_vs_human <- subset(data, treatment %in% c("LLM", "Human"))
model_rq3 <- lmer(polarization_diff ~ treatment + tweet_bias + political_leaning +
(1 | participant_id),
data = data_llm_vs_human)
summary(model_rq3)
report(model_rq2)
# Polarization difference (original - treated)
data$polarization_diff <- data$original_polarization - data$treated_polarization
# Compare LLM vs. Placebo
data_llm_vs_placebo <- subset(data, treatment %in% c("LLM", "Placebo"))
model_rq2 <- lmer(polarization_diff ~ treatment + tweet_bias + political_leaning +
(1 | participant_id),
data = data_llm_vs_placebo)
summary(model_rq2)
report(model_rq2)
# Compare LLM vs. Human
data_llm_vs_human <- subset(data, treatment %in% c("LLM", "Human"))
model_rq3 <- lmer(polarization_diff ~ treatment + tweet_bias + political_leaning +
(1 | participant_id),
data = data_llm_vs_human)
summary(model_rq3)
report(model_rq3)
model_rq4 <- lmer(original_polarization ~ tweet_bias * political_leaning +
(1 | participant_id), data = data)
summary(model_rq4)
report(model_rq4)
library(lmerTest)
library(lme4)
library(report)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
sample(survey_data)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
sample(survey_data, 10)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
sample(survey_data, n=10)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
sample(survey_data, size=10)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
sample(survey_data)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
sample(n=10, survey_data)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
sample(size=10, survey_data)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
sample(survey_data)
m_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_rq1)
report(m_rq1)
m_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning * TweetBias + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_rq1)
report(m_rq1)
m_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning + TweetBias + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_rq1)
report(m_rq1)
data_llm_vs_placebo <- subset(survey_data, TreatmentGroup %in% c("machine", "placebo"))
model_rq2 <- lmer(DiffLikertTreatedOriginal ~ TreatementGroup + TweetBias + PoliticalLeaning +
(1 | FK_ParticipantId),
data = data_llm_vs_placebo)
data_llm_vs_placebo <- subset(survey_data, TreatmentGroup %in% c("machine", "placebo"))
model_rq2 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup + TweetBias + PoliticalLeaning +
(1 | FK_ParticipantId),
data = data_llm_vs_placebo)
data_llm_vs_placebo <- subset(survey_data, TreatmentGroup %in% c("machine", "placebo"))
model_rq2 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup + TweetBias + ParticipantLeaning +
(1 | FK_ParticipantId),
data = data_llm_vs_placebo)
summary(model_rq2)
report(model_rq2)
data_llm_vs_placebo <- subset(survey_data, TreatmentGroup %in% c("machine", "placebo"))
model_rq2 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup + TweetBias * ParticipantLeaning +
(1 | FK_ParticipantId),
data = data_llm_vs_placebo)
summary(model_rq2)
report(model_rq2)
# Compare LLM vs. Human
data_llm_vs_human <- subset(survey_data, TreatmentGroup %in% c("machine", "human"))
model_rq3 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup + TweetBias + ParticipantLeaning +
(1 | participant_id),
data = FK_ParticipantId)
# Compare LLM vs. Human
data_llm_vs_human <- subset(survey_data, TreatmentGroup %in% c("machine", "human"))
model_rq3 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup + TweetBias + ParticipantLeaning +
(1 | FK_ParticipantId),
data = data_llm_vs_human)
summary(model_rq3)
report(model_rq3)
model_rq4 <- lmer(OriginalLikertValue ~ TweetBias * ParticipantLeaning +
(1 | FK_ParticipantId), data = data)
model_rq4 <- lmer(OriginalLikertValue ~ TweetBias * ParticipantLeaning +
(1 | FK_ParticipantId), data = survey_data)
summary(model_rq4)
report(model_rq4)
library(lmerTest)
library(lme4)
library(report)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
sample(survey_data)
m_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning * TweetBias + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_rq1)
m_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning + TweetBias + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_rq1)
report(m_rq1)
data_llm_vs_placebo <- subset(survey_data, TreatmentGroup %in% c("machine", "placebo"))
model_rq2 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup + TweetBias * ParticipantLeaning +
(1 | FK_ParticipantId),
data = data_llm_vs_placebo)
summary(model_rq2)
report(model_rq2)
# Compare LLM vs. Human
data_llm_vs_human <- subset(survey_data, TreatmentGroup %in% c("machine", "human"))
model_rq3 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup + TweetBias + ParticipantLeaning +
(1 | FK_ParticipantId),
data = data_llm_vs_human)
summary(model_rq3)
report(model_rq3)
model_rq4 <- lmer(OriginalLikertValue ~ TweetBias * ParticipantLeaning +
(1 | FK_ParticipantId), data = survey_data)
summary(model_rq4)
report(model_rq4)
# try without leaning and bias
# try using only machine group
m_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_rq1)
report(m_rq1)
# try without leaning and bias
# try using only machine group
m_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning + TweetBias + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_rq1)
# try without leaning and bias
# try using only machine group
m_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning + TweetBias + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_rq1)
model_multi <- lmer(
OriginalLikertValue ~ TreatedLikertValue * TreatmentGroup + (1 | FK_ParticipantId),
data=survey_data
)
summary(model_multi)
report(model_multi)
# try without leaning and bias
# try using only machine group
m_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning + TweetBias + (1 | FK_ParticipantId) + (1 | Text1),
data=survey_data, family = "binomial")
summary(m_rq1)
# try without leaning and bias
# try using only machine group
m_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning + TweetBias + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_rq1)
# try without leaning and bias
# try using only machine group
m_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning + TweetBias + (0 | FK_ParticipantId),
data=survey_data, family = "binomial")
# try without leaning and bias
# try using only machine group
m_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning + TweetBias + (4 | FK_ParticipantId),
data=survey_data, family = "binomial")
# try without leaning and bias
# try using only machine group
m_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning + TweetBias + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_rq1)
data_llm_vs_placebo <- subset(survey_data, TreatmentGroup %in% c("machine", "placebo"))
model_rq2 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup + TweetBias * ParticipantLeaning +
(1 | FK_ParticipantId),
data = data_llm_vs_placebo)
summary(model_rq2)
report(model_rq2)
# try without leaning and bias
# try using only machine group
m_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning + TweetBias + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_rq1)
report(m_rq1)
report(m_general)
m_general = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning + TweetBias + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_general)
report(m_general)
print("teste")
survey_data$TweetBias
count(survey_data$TweetBias)
n(survey_data$TweetBias)
n(survey_data$TweetBias)
count(survey_data$TweetBias)
survey_data$TweetBias
#survey_data$TweetBias <- ifelse(survey_data$TweetBias == "L", "left", "right")
survey_data$TweetBias <- ifelse(survey_data$TweetBias == "L", "left", "right")
survey_data$TweetBias
library(lmerTest)
library(lme4)
library(report)
library(hash)
install.packages("hash")
library(lmerTest)
library(lme4)
library(report)
library(hash)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
sample(survey_data)
library(lmerTest)
library(lme4)
library(report)
library(lme4)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
sample(survey_data)
library(lmerTest)
library(lme4)
library(report)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
sample(survey_data)
m_general = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning + TweetBias + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_general)
report(m_general)
m_general = glmer(TreatedIsLessPolar ~ TreatmentGroup * ParticipantLeaning * TweetBias + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_general)
m_general = glmer(TreatedIsLessPolar ~ TreatmentGroup + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_general)
report(m_general)
#model_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning + TweetBias + (1 | FK_ParticipantId),
#              data=survey_data, family = "binomial")
machine_placebo <- subset(survey_data, TreatmentGroup %in% c("machine", "placebo"))
model_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup (1 | FK_ParticipantId),
data=machine_placebo, family = "binomial")
#model_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning + TweetBias + (1 | FK_ParticipantId),
#              data=survey_data, family = "binomial")
machine_placebo <- subset(survey_data, TreatmentGroup %in% c("machine", "placebo"))
model_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + (1 | FK_ParticipantId),
data=machine_placebo, family = "binomial")
summary(model_rq1)
report(model_rq1)
#model_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning + TweetBias + (1 | FK_ParticipantId),
#              data=survey_data, family = "binomial")
machine_placebo <- subset(survey_data, TreatmentGroup %in% c("machine", "placebo"))
model_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + (1 | FK_ParticipantId),
data=machine_placebo, family = "binomial")
summary(model_rq1)
report(model_rq1)
machine_placebo <- subset(survey_data, TreatmentGroup %in% c("machine", "placebo"))
model_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning + TweetBias + (1 | FK_ParticipantId),
data=machine_placebo, family = "binomial")
summary(model_rq1)
machine_placebo <- subset(survey_data, TreatmentGroup %in% c("machine", "placebo"))
model_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + ParticipantLeaning * TweetBias + (1 | FK_ParticipantId),
data=machine_placebo, family = "binomial")
summary(model_rq1)
machine_placebo <- subset(survey_data, TreatmentGroup %in% c("machine", "placebo"))
model_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + (1 | FK_ParticipantId),
data=machine_placebo, family = "binomial")
summary(model_rq1)
report(model_rq1)
anov = anova(model_rq1)
summary(anov)
report(anov)
anov = anova(model_rq1)
report(anov)
model_rq2 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup + TweetBias * ParticipantLeaning +
(1 | FK_ParticipantId),
data = machine_placebo)
summary(model_rq2)
report(model_rq2)
#model_rq2 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup + TweetBias * ParticipantLeaning +
#                  (1 | FK_ParticipantId),
#                  data = machine_placebo)
model_rq2 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup +(1 | FK_ParticipantId),
data = machine_placebo)
summary(model_rq2)
report(model_rq2)
# Compare LLM vs. Human
machine_human <- subset(survey_data, TreatmentGroup %in% c("machine", "human"))
model_rq3 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup + TweetBias + ParticipantLeaning +
(1 | FK_ParticipantId),
data = machine_human)
summary(model_rq3)
# Compare LLM vs. Human
machine_human <- subset(survey_data, TreatmentGroup %in% c("machine", "human"))
model_rq3 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup (1 | FK_ParticipantId),
data = machine_human)
# Compare LLM vs. Human
machine_human <- subset(survey_data, TreatmentGroup %in% c("machine", "human"))
model_rq3 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup + (1 | FK_ParticipantId),
data = machine_human)
summary(model_rq3)
report(model_rq3)
model_rq4 <- lmer(OriginalLikertValue ~ TweetBias * ParticipantLeaning +
(1 | FK_ParticipantId), data = survey_data)
summary(model_rq4)
report(model_rq4)
m_general = glmer(TreatedIsLessPolar ~ relevel(TreatmentGroup, ref="machine") + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
m_general = glmer(TreatedIsLessPolar ~ relevel(as.factor(TreatmentGroup), ref="machine") + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_general)
report(m_general)
m_general = glmer(TreatedIsLessPolar ~ TreatmentGroup + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_general)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
survey_data$TreatmentGroup <- relevel(survey_data$TreatmentGroup, ref = "machine")
class(survey_data$TreatmentGroup)
class(as.factor(survey_data$TreatmentGroup))
as.factor(survey_data$TreatmentGroup
as.factor(survey_data$TreatmentGroup)
levels(survey_data$TreatmentGroup)
levels(as.factor(survey_data$TreatmentGroup))
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
survey_data$TreatmentGroup <- relevel(as.factor(survey_data$TreatmentGroup, ref = "machine"))
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
survey_data$TreatmentGroup <- relevel(as.factor(survey_data$TreatmentGroup, ref = "machine")))
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
survey_data$TreatmentGroup <- as.factor(survey_data$TreatmentGroup)
survey_data$TreatmentGroup <- relevel(survey_data$TreatmentGroup, ref = "machine")
sample(survey_data)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
survey_data$TreatmentGroup <- as.factor(survey_data$TreatmentGroup)
survey_data$TreatmentGroup <- relevel(survey_data$TreatmentGroup, ref = "machine")
sample(survey_data)
m_general = glmer(TreatedIsLessPolar ~ TreatmentGroup + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_general)
report(m_general)
library(lmerTest)
library(lme4)
library(report)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
survey_data$TreatmentGroup <- as.factor(survey_data$TreatmentGroup)
survey_data$TreatmentGroup <- relevel(survey_data$TreatmentGroup, ref = "machine")
sample(survey_data)
m_general = glmer(TreatedIsLessPolar ~ TreatmentGroup + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_general)
report(m_general)
machine_placebo <- subset(survey_data, TreatmentGroup %in% c("machine", "placebo"))
model_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + (1 | FK_ParticipantId),
data=machine_placebo, family = "binomial")
summary(model_rq1)
report(model_rq1)
#model_rq2 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup + TweetBias * ParticipantLeaning +
#                  (1 | FK_ParticipantId),
#                  data = machine_placebo)
model_rq2 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup +(1 | FK_ParticipantId),
data = machine_placebo)
summary(model_rq2)
report(model_rq2)
# Compare LLM vs. Human
machine_human <- subset(survey_data, TreatmentGroup %in% c("machine", "human"))
model_rq3 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup + (1 | FK_ParticipantId),
data = machine_human)
summary(model_rq3)
report(model_rq3)
model_rq4 <- lmer(OriginalLikertValue ~ TweetBias * ParticipantLeaning +
(1 | FK_ParticipantId), data = survey_data)
summary(model_rq4)
report(model_rq4)
model_multi <- lmer(
OriginalLikertValue ~ TreatedLikertValue * TreatmentGroup + (1 | FK_ParticipantId),
data=survey_data
)
summary(model_multi)
report(model_multi)
model_rq4 <- lmer(OriginalLikertValue ~ TweetBias * ParticipantLeaning +
(1 | FK_ParticipantId), data = survey_data)
summary(model_rq4)
report(model_rq4)
machine_placebo <- subset(survey_data, TreatmentGroup %in% c("machine", "placebo"))
model_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + (1 | FK_ParticipantId),
data=machine_placebo, family = "binomial")
summary(model_rq1)
report(model_rq1)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
survey_data$TreatmentGroup <- as.factor(survey_data$TreatmentGroup)
survey_data$TreatmentGroup <- relevel(survey_data$TreatmentGroup, ref = "machine")
sample(survey_data, n = 10)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
survey_data$TreatmentGroup <- as.factor(survey_data$TreatmentGroup)
survey_data$TreatmentGroup <- relevel(survey_data$TreatmentGroup, ref = "machine")
head(survey_data, n = 10)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
survey_data$TreatmentGroup <- as.factor(survey_data$TreatmentGroup)
survey_data$TreatmentGroup <- relevel(survey_data$TreatmentGroup, ref = "machine")
#head(survey_data, n = 10)
install.packages("tinytex")
install.packages("tinytex")
tinytex::install_tinytex()
update.packages("xfun")
tinytex::install_tinytex()
tinytex::install_tinytex()
library(lmerTest)
library(lme4)
library(report)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
survey_data$TreatmentGroup <- as.factor(survey_data$TreatmentGroup)
survey_data$TreatmentGroup <- relevel(survey_data$TreatmentGroup, ref = "machine")
#head(survey_data, n = 10)
m_general = glmer(TreatedIsLessPolar ~ TreatmentGroup + (1 | FK_ParticipantId),
data=survey_data, family = "binomial")
summary(m_general)
report(m_general)
machine_placebo <- subset(survey_data, TreatmentGroup %in% c("machine", "placebo"))
model_rq1 = glmer(TreatedIsLessPolar ~ TreatmentGroup + (1 | FK_ParticipantId),
data=machine_placebo, family = "binomial")
summary(model_rq1)
report(model_rq1)
#model_rq2 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup + TweetBias * ParticipantLeaning +
#                  (1 | FK_ParticipantId),
#                  data = machine_placebo)
model_rq2 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup +(1 | FK_ParticipantId),
data = machine_placebo)
summary(model_rq2)
report(model_rq2)
# Compare LLM vs. Human
machine_human <- subset(survey_data, TreatmentGroup %in% c("machine", "human"))
model_rq3 <- lmer(DiffLikertTreatedOriginal ~ TreatmentGroup + (1 | FK_ParticipantId),
data = machine_human)
summary(model_rq3)
report(model_rq3)
model_rq4 <- lmer(OriginalLikertValue ~ TweetBias * ParticipantLeaning +
(1 | FK_ParticipantId), data = survey_data)
summary(model_rq4)
report(model_rq4)
survey_data <- read.csv('../../../backend/data/database/survey_data.csv')
survey_data$TreatmentGroup <- as.factor(survey_data$TreatmentGroup)
survey_data$TreatmentGroup <- relevel(survey_data$TreatmentGroup, ref = "machine")
head(survey_data, n = 10)
survey_data$IsCoherent <- ifelse(survey_data$AnswerQ3 > 3, 1, 0)
model_cohesion <- lmer(IsCoherent ~ TreatmentGroup + (1 | FK_ParticipantId),
data=survey_data)
summary(model_cohesion)
survey_data$IsCoherent <- ifelse(survey_data$AnswerQ3 > 3, 1, 0)
model_cohesion <- lmer(IsCoherent ~ TreatmentGroup + (1 | FK_ParticipantId),
data=survey_data)
summary(model_cohesion)
report(model_cohesion)
report(model_cohesion)
